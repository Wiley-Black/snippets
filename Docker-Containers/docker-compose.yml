services:
  mine:
    build: mine
    image: mine:latest
    
    # Make some storage from outside of the container available within the container.
    # Changes made here from inside the container will persist beyond the container lifetime.
    volumes:
      - type: bind
        source: H:\
        target: /mnt/persist

    # Connect using Remote Desktop to localhost:3390.
    ports:
      - "3390:3389"

    # To do interactive troubleshooting:
    #   docker compose run --rm ubuntu bash
    stdin_open: true # Equivalent to docker run -i
    tty: true        # Equivalent to docker run -t
    
    # VS Code wants to sandbox and needs access to the host IPC and (I think) PID access
    ipc: host # Enables sharing of the host's IPC namespace
    pid: host # Shares the host's PID namespace, allowing container to see and interact with all processes
